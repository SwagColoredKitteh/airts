[package]
name = "rts"
version = "0.1.0"
authors = ["SwagColoredKitteh <kittycake@openmailbox.org>"]

[features]
default = ["piston_renderer"]
piston_renderer = ["piston_window"]

[dependencies]
piston_window = { optional = true, version = "*" }
